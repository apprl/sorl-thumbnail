#!/bin/sh

# Zip all old imports

su -s /bin/sh -c "/bin/gzip -q -r %(path)s/shared/warehouse"

# Run import for all configured apparelrow providers, NOT in parallel

su -s /bin/sh -c "%(path)s/bin/import 1>> %(path)s/var/logs/importer.log" %(run_user)s
