#!/bin/sh

# Zip all old imports

su -s /bin/sh -c "/bin/gzip -q -r /home/deploy/apparelrow/shared/warehouse"

# Run import for all configured apparelrow providers

su -s /bin/sh -c "/home/deploy/apparelrow/bin/import 1>> /home/deploy/apparelrow/var/logs/importer.log" www-data
