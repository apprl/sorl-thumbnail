#!/bin/bash

if [[ -z $PROJDIR ]]; then
    PROJDIR=`pwd`
fi

PROJNAME='apparelrow'                           # Django project name

export PYTHONPATH="$PROJDIR/$PROJNAME"
export DJANGO_SETTINGS_MODULE=settings


if [[ -e $PROJDIR/python/bin/activate ]]; then
    . $PROJDIR/python/bin/activate
fi
cd $PROJDIR/lib

python runner.py $@


