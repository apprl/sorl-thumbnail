"""
The importer.provider.abstract namespace contains implementations of general
provider formats (mappers and parsers) that concrete providers may extend from

Examples of this are the ApparelRow format, or the PriceRunner format.

"""
pass
